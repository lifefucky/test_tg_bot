[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
# macOS + системный Python: LibreSSL старше OpenSSL 1.1.1 → urllib3 предупреждает (транзитивно из requests).
filterwarnings =
    ignore::urllib3.exceptions.NotOpenSSLWarning
